pipeline {
    agent any
    stages{
        stage("Multibranch_Example"){
            when{
                branch 'deployement'
                changeset '*.java'
            }
            steps{
                echo "$env.BRANCH_NAME"
                
                build 'declarative_syntax_poc'
            }
        }
    }
}
