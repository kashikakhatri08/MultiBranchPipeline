pipeline {
    agent any
    stages{
        stage("Multibranch_Example"){
            when{
                branch 'development'
                changeset '*.java'
            }
            steps{
                echo "$env.BRANCH_NAME"
                
                build 'input_poc'
            }
        }
    }
}
