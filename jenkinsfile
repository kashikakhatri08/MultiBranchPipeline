pipeline {
    agent any
    stages{
        stage("Multibranch_Example"){
            when{
                
                changeset '*.java'
            }
            steps{
                echo "$env.BRANCH_NAME"
                
                build 'input_poc'
            }
        }
    }
}
